#!/usr/bin/env -S mise exec -- bun
import { lint } from "../impl/js/lint.js";

const { out, errors } = lint(Bun.argv.slice(2), (p) => require("fs").readFileSync(p, "utf8"));
process.stderr.write(out.join("\n") + "\n");
process.exit(errors > 0 ? 1 : 0);
