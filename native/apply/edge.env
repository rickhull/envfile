# apply/edge.env — native apply fixture
# baseline env:
#   A=1
#   HOME=/home/user
#   PATH=/usr/local/bin:/usr/bin:/bin

A=alpha
BRACED=${HOME}/bin
CHAIN=$A:${HOME}:${PATH}
LITERAL=$$HOME
PRICE=$9
SELF=$SELF
